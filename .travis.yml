language: go

service:
  - docker

go:
  - 1.15.5

before_install:
  - curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b $(go env GOPATH)/bin v1.24.0

script:
  - docker build -t yegamble/tryte-crypt-go .

deploy:
  provider: script
  script: bash .docker_push
  on:
    branch: master